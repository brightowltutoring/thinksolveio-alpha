// import { createContext } from "@/utils";
// import { createContextFn } from "@/utils";

// class Counter {
// 	value = $state(0);
// 	incr = () => this.value++
// 	decr = () => this.value--
// 	reset = () => this.value = 0
// }
// function CounterFn() {
// 	let value = $state(0);
// 	return {
// 		get value() { return value },
// 		incr: () => value++,
// 		decr: () => value--,
// 		reset: () => value = 0

// 	}
// }

// export const [appProvider, appConsumer] = createContext(CounterFn)
// export const [Provider, Consumer] = createContext<() => number>()



